# Database
DB_PASSWORD=change-me-in-production
POSTGRES_USER=geget
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=geget

# Backend
PORT=8080
ENVIRONMENT=production
DATABASE_URL=postgres://geget:${DB_PASSWORD}@db:5432/geget?sslmode=disable

# SMTP (for contact form notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@geget.org

# Frontend
NEXT_PUBLIC_API_URL=https://geget.org
NEXT_PUBLIC_SITE_URL=https://geget.org

# JWT (Phase 2)
JWT_SECRET=change-me-in-production
JWT_EXPIRY=24h
